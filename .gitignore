.gradle/
**/build/
**/target/
onyxsdk-pen/src/main/jniLibs/
*.iml
.idea/
.DS_Store
local.properties
recovery-evidence/
# Local reference artifacts: original binaries stay outside version control
# as a matter of policy; ignoring the conventional local names keeps an
# accidental `git add .` from tracking them.
libneo_pen.so
onyxsdk-pen-native-classes.jar
__pycache__/
# Raw device-validation runs stay local; promote_fixture.py copies the
# sanitized, deterministic subset into device-validation/fixtures/ (tracked).
device-validation/results/
