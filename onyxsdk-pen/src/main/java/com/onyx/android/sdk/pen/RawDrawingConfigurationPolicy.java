package com.onyx.android.sdk.pen;

/** Controls whether toggling raw drawing overwrites firmware brush/eraser configuration. */
public enum RawDrawingConfigurationPolicy {
    RESET_TO_FIRMWARE_DEFAULTS,
    PRESERVE_CURRENT
}
